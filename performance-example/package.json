{
  "name": "performance-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start:pm2": "npx pm2 start server.js -l logs.txt -i max",
    "stop:pm2": "npx pm2 stop",
    "delete:pm2": "npx pm2 delete",
    "restart:pm2": "npx pm2 restart",
    "logs:pm2": "npx pm2 logs",
    "show:pm2": "npx pm2 show",
    "show:monit:pm2": "npx pm2 monit",
    "reload:pm2": "npx pm2 reload server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "pm2": "^5.2.0"
  }
}
